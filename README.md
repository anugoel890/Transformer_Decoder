# Transformer_Decoder for LLMs
The objectives are:
1) Create a transformer decoder from scratch (like GPT) for text generation.
2) Test with different hyperparameters.
3) Generate relevant text using GPT models from HuggingFace.
